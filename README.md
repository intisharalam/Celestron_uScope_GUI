The goal:
1. Open a camera (a device that can be read like a file)
2. Repeatedly: read one image (a "frame") from it
3. Optionally transform that image (zoom, crop, brightness)
4. Show it on screen
5. Sometimes save it (snapshot or video)
